const solution = require("../src/solution");

describe("Solution tests", () => {
    it("GIVEN WHEN THEN", () => {
        // given
        const expectedResult = false;

        // when
        const actualResult = solution.testFunction();

        // then
        expect(actualResult).toEqual(expectedResult);
    })
})